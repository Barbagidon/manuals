  onToggleProp = (id, prop) => {    -  method

        this.setState(({ data }) => ({
            data: data.map(item => {
                if (item.id === id) {
                    return { ...item, [prop]: !item[prop] }
                } else {
                    return item;
                }

            })

        }))

    }
    
    set data attribute with required value and get it when the function will start:
     <button type="button"
                    className="btn-cookie btn-sm"
                    onClick={onToggleProp}
                    data-toggle='increase'>   ---- our attribute
                    <i className="fas fa-cookie"></i>
                </button>

 onToggleProp = {(e)=> onToggleProp(id, e.currentTarget.getAttribute('data-toggle'))} - get it
